version = "1.2.0"
description = "Run superuser commands though Discord!"

aliucord {
    changelog.set(
            """
            # 1.2.0
            * Add ability to send (default is false)
            # 1.1.0
            * Added support for custom mkshrc. (loads `/system/etc/mkshrc`)
            # 1.0.0
            * Initial release
        """.trimIndent()
    )
}